<div class="sidebar @(collapseNavMenu ? "" : "open")">
    <div class="brand-row">
        <div class="brand-mark">DL</div>
        <a class="navbar-brand" href="">DMS &amp; Logistics</a>
    </div>

    <button class="navbar-toggler" title="Mở menu" @onclick="ToggleNavMenu">☰</button>

    <nav class="nav-scrollable">
        <div class="nav-group-label">Tổng quan</div>
        <div class="nav-item">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="nav-icon">🏠</span> Tổng quan sản phẩm
            </NavLink>
        </div>

        <div class="nav-group-label">Vận hành</div>
        <div class="nav-item">
            <NavLink class="nav-link" href="dashboard">
                <span class="nav-icon">📊</span> Báo cáo quản trị
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="san-pham">
                <span class="nav-icon">🏷️</span> Quản lý sản phẩm
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="dat-hang">
                <span class="nav-icon">🛒</span> Đặt hàng
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="van-chuyen">
                <span class="nav-icon">🚚</span> Vận chuyển
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
