@page "/"

<PageTitle>DMS & Logistics — Tổng quan</PageTitle>

<div class="hero">
    <h1>Hệ thống quản lý phân phối &amp; vận chuyển</h1>
    <p>Toàn bộ chuỗi phân phối — từ đặt hàng, tồn kho, công nợ đến giao hàng — theo dõi trên cùng một hệ thống.</p>
    <div class="hero-actions">
        <a class="btn-primary" href="dashboard">Xem báo cáo quản trị →</a>
        <a class="btn-outline" href="dat-hang">Thử đặt hàng</a>
    </div>
</div>

<h2 class="section-title">4 module cốt lõi</h2>
<div class="module-grid">
    <div class="module-card">
        <div class="module-emoji">💰</div>
        <h3>Bán hàng &amp; Đặt hàng</h3>
        <p>Nhân viên thị trường tạo đơn cho nhà phân phối theo giá sỉ, hoặc khách tự đặt hàng lẻ. Tự động tính chiết khấu theo bậc số lượng.</p>
        <a href="dat-hang">Xem demo →</a>
    </div>
    <div class="module-card">
        <div class="module-emoji">📦</div>
        <h3>Kho &amp; Tồn kho</h3>
        <p>Theo dõi mức tồn kho tại từng nhà phân phối theo thời gian thực, cảnh báo sớm khi sắp hết hàng hoặc tồn dư.</p>
        <a href="dashboard">Xem demo →</a>
    </div>
    <div class="module-card">
        <div class="module-emoji">💳</div>
        <h3>Công nợ &amp; Tài chính</h3>
        <p>Quản lý hạn mức tín dụng theo từng nhà phân phối, cảnh báo ngay khi công nợ vượt hạn mức hoặc quá hạn.</p>
        <a href="dashboard">Xem demo →</a>
    </div>
    <div class="module-card">
        <div class="module-emoji">🚚</div>
        <h3>Vận chuyển &amp; Giao hàng</h3>
        <p>Theo dõi từng đơn hàng đang vận chuyển: vị trí trên tuyến đường, thời gian dự kiến, cảnh báo trễ hẹn.</p>
        <a href="van-chuyen">Xem demo →</a>
    </div>
</div>

<style>
    .hero { text-align: center; padding: 3rem 1rem; }
    .hero h1 { font-size: 2rem; margin-bottom: 0.75rem; }
    .hero p { color: #64748b; max-width: 640px; margin: 0 auto 1.5rem; }
    .hero-actions { display: flex; gap: 0.75rem; justify-content: center; flex-wrap: wrap; }
    .btn-primary { background: #2563eb; color: #fff; padding: 0.7rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; }
    .btn-outline { border: 1px solid #cbd5e1; color: #334155; padding: 0.7rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; }
    .section-title { text-align: center; margin: 2.5rem 0 1.5rem; }
    .module-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1.25rem; padding: 0 1rem 2rem; }
    .module-card { border: 1px solid #e2e8f0; border-radius: 12px; padding: 1.25rem; background: #f8fafc; }
    .module-emoji { font-size: 1.8rem; margin-bottom: 0.5rem; }
    .module-card h3 { margin: 0 0 0.5rem; font-size: 1.05rem; }
    .module-card p { color: #64748b; font-size: 0.9rem; margin-bottom: 0.75rem; }
    .module-card a { color: #2563eb; font-weight: 600; font-size: 0.9rem; text-decoration: none; }
</style>
